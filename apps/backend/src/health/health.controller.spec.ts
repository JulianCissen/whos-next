import { describe, it, expect } from 'vitest';

describe('HealthController', () => {
  it('is importable (placeholder — confirms Vitest runner executes)', () => {
    // This placeholder test runs with zero assertions to confirm the Vitest
    // runner starts correctly for the backend package.
    // Unit tests for HealthController logic will be added in Unit 1+.
    expect(true).toBe(true);
  });
});
