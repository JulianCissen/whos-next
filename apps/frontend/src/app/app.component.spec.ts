import { describe, it, expect } from 'vitest';

describe('AppComponent', () => {
  it('placeholder — confirms Vitest + Angular runner executes', () => {
    // This placeholder test confirms the frontend Vitest runner starts correctly.
    // Component unit tests will be added in Unit 1+ as features are implemented.
    expect(true).toBe(true);
  });
});
