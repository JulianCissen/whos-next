# Copilot Instructions — Who's Next

## Sign-off checklist

Before signing off on any change, verify:

1. **No type errors** — the TypeScript compiler reports zero errors across all affected packages.
2. **No lint errors or warnings** — ESLint reports zero errors and zero warnings across all affected files.
